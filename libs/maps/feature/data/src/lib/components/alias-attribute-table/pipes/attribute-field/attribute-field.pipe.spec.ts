import { AttributeFieldPipe } from './attribute-field.pipe';

describe('AttributeFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
