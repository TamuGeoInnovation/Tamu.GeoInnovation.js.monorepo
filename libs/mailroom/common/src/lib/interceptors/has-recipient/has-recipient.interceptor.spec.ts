import { HasRecipientInterceptor } from './has-recipient.interceptor';

describe('HasRecipientInterceptor', () => {
  it('should be defined', () => {
    expect(new HasRecipientInterceptor()).toBeDefined();
  });
});
