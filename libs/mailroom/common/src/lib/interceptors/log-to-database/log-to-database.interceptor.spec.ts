import { LogToDatabaseInterceptor } from './log-to-database.interceptor';

describe('LogToDatabaseInterceptor', () => {
  it('should be defined', () => {
    expect(new LogToDatabaseInterceptor()).toBeDefined();
  });
});
