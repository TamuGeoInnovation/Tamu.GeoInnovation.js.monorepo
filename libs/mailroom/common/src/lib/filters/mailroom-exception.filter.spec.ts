import { MailroomExceptionFilter } from './mailroom-exception.filter';

describe('MailroomExceptionFilter', () => {
  it('should be defined', () => {
    expect(new MailroomExceptionFilter()).toBeDefined();
  });
});
