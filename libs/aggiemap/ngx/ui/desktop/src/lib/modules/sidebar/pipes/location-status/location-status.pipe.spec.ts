import { LocationStatusPipe } from './location-status.pipe';

describe('LocationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
