import { CategoryStatusPipe } from './category-status.pipe';

describe('CategoryStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
