import { TileSubmenuDirective } from './tile-submenu.directive';

describe('TileSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new TileSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
