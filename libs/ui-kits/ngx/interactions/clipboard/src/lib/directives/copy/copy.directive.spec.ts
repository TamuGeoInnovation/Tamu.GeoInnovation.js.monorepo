import { ClipboardCopyDirective } from './copy.directive';

describe('ClipboardCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new ClipboardCopyDirective();
    expect(directive).toBeTruthy();
  });
});
