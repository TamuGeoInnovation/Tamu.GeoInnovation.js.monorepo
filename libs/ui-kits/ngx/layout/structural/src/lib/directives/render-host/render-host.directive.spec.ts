import { RenderHostDirective } from './render-host.directive';

describe('RenderHostDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderHostDirective();
    expect(directive).toBeTruthy();
  });
});
