import { IsLimitPipe } from './is-limit.pipe';

describe('IsLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
