import { StepperToggleDirective } from './stepper-toggle.directive';

describe('StepperToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperToggleDirective();
    expect(directive).toBeTruthy();
  });
});
