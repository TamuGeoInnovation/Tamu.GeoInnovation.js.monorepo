import { SubmissionReviewStatusPipe } from './submission-review-status.pipe';

describe('SubmissionReviewStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionReviewStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
