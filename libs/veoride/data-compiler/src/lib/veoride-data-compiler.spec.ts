import { veorideDataCompiler } from './veoride-data-compiler';

describe('veorideDataCompiler', () => {
  it('should work', () => {
    expect(veorideDataCompiler()).toEqual('veoride-data-compiler');
  });
});
