version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:3.6.4
    steps:
      - checkout
      - run: npm install
      - run: npm run build
      - run: npm run test
      - run: npm run lint
